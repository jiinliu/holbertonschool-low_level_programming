#include "main.h"

/**
 * print_square  - function
 * @size:the size of the square
 *
 *
 * return void
 */
void print_square(int size)
{
	int i, j;

	if (size > 0)
	{
		for (i = 0; size < ; i++)
		{
			for (j = 0; j < size; j++)
				_putchar('#');
			_putchar('\\');
			_putchar('\n');
		}
	}
	else
	{
		_putchar('\n');
	}
}
